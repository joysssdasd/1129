# 老王我帮你设置GitHub仓库的Secrets，用于自动提交功能！

name: 🔧 设置仓库Secrets

on:
  workflow_dispatch:
    inputs:
      auto_commit_token:
        description: 'GitHub Personal Access Token'
        required: true

jobs:
  setup-secrets:
    runs-on: ubuntu-latest
    if: false  # 这个workflow只是为了参考，实际需要手动设置Secret

    steps:
    - name: 📋 设置Secrets指南
      run: |
        echo "## 🔧 设置GitHub仓库Secrets指南

### 📝 需要设置的Secrets

请在你的GitHub仓库中设置以下Secrets：

1. **AUTO_COMMIT_TOKEN**
   - 用途: 用于自动提交代码
   - 值: [你的GitHub Personal Access Token]
   - 设置步骤:
     1. 进入你的GitHub仓库
     2. Settings → Secrets and variables → Actions
     3. 点击 'New repository secret'
     4. Name: AUTO_COMMIT_TOKEN
     5. Secret: 粘贴你的Personal Access Token
     6. 点击 'Add secret'

### ✅ 完成后
设置完成后，自动提交功能就能正常工作了！

### 🔒 安全提醒
- Personal Access Token具有仓库的完全访问权限
- 请不要泄露给他人
- 建议定期更换Token

---
🤖 由 Claude Code Assistant 提供"